<Window x:Class="Football_Management_System.MatchResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản lý kết quả trận đấu" 
        Height="600" 
        Width="950"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#1565C0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="⚽" FontSize="28" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="QUẢN LÝ KẾT QUẢ TRẬN ĐẤU" 
                               Foreground="White" 
                               FontSize="22" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Thống kê nhanh -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Border Background="#0D47A1" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Tổng trận: " Foreground="#B3E5FC"/>
                            <TextBlock x:Name="txtTongTran" Text="0" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#1B5E20" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Đã có KQ: " Foreground="#C8E6C9"/>
                            <TextBlock x:Name="txtDaCoKQ" Text="0" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: FORM NHẬP -->
            <Border Grid.Column="0" Background="#FAFAFA" CornerRadius="8" Margin="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        
                        <!-- Tiêu đề form -->
                        <TextBlock Text="📝 THÔNG TIN TRẬN ĐẤU" FontWeight="Bold" FontSize="14" 
                                   Foreground="#1565C0" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Chọn Trận Đấu" Margin="0,0,0,3" FontWeight="SemiBold"/>
                        <ComboBox x:Name="cboMatch" 
                                  Height="30"
                                  SelectionChanged="cboMatch_SelectionChanged"/>

                        <TextBlock Text="Đội Nhà" Margin="0,12,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtHomeTeam" Height="30" Padding="5"/>

                        <TextBlock Text="Đội Khách" Margin="0,12,0,3" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtAwayTeam" Height="30" Padding="5"/>

                        <TextBlock Text="Ngày Thi Đấu" Margin="0,12,0,3" FontWeight="SemiBold"/>
                        <DatePicker x:Name="dpMatchDate" Height="30"/>

                        <!-- Tỷ số -->
                        <TextBlock Text="⚽ TỶ SỐ" FontWeight="Bold" FontSize="13" 
                                   Foreground="#2E7D32" Margin="0,15,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Đội Nhà" Margin="0,0,0,3" HorizontalAlignment="Center"/>
                                <TextBox x:Name="txtHomeScore" Height="35" FontSize="18" FontWeight="Bold"
                                         HorizontalContentAlignment="Center" Padding="5"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Column="1" Text="-" FontSize="24" FontWeight="Bold"
                                       HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                            
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Đội Khách" Margin="0,0,0,3" HorizontalAlignment="Center"/>
                                <TextBox x:Name="txtAwayScore" Height="35" FontSize="18" FontWeight="Bold"
                                         HorizontalContentAlignment="Center" Padding="5"/>
                            </StackPanel>
                        </Grid>

                        <!-- Thẻ phạt -->
                        <TextBlock Text="🟨 THẺ PHẠT" FontWeight="Bold" FontSize="13" 
                                   Foreground="#F57C00" Margin="0,15,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,5,5">
                                <TextBlock Text="🟨 Vàng (Nhà)" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtHomeYellow" Height="28" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="5,0,0,5">
                                <TextBlock Text="🟨 Vàng (Khách)" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtAwayYellow" Height="28" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,5,0">
                                <TextBlock Text="🟥 Đỏ (Nhà)" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtHomeRed" Height="28" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
                                <TextBlock Text="🟥 Đỏ (Khách)" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtAwayRed" Height="28" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <!-- Ghi chú -->
                        <TextBlock Text="📋 GHI CHÚ" FontWeight="Bold" FontSize="13" 
                                   Foreground="#5E35B1" Margin="0,15,0,8"/>
                        <TextBox x:Name="txtNote" Height="50" TextWrapping="Wrap" 
                                 AcceptsReturn="True" Padding="5"/>

                        <!-- BUTTONS -->
                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                            <Button x:Name="btnAdd" Content="➕ Thêm" 
                                    Width="70" Height="35" 
                                    Background="#2196F3" Foreground="White"
                                    Margin="0,0,5,0" FontWeight="SemiBold"
                                    Click="btnAdd_Click"/>
                            <Button x:Name="btnEdit" Content="✏️ Sửa" 
                                    Width="70" Height="35" 
                                    Background="#FF9800" Foreground="White"
                                    Margin="0,0,5,0" FontWeight="SemiBold"
                                    Click="btnEdit_Click"/>
                            <Button x:Name="btnDelete" Content="🗑️ Xóa" 
                                    Width="70" Height="35"
                                    Background="#f44336" Foreground="White"
                                    Margin="0,0,5,0" FontWeight="SemiBold"
                                    Click="btnDelete_Click"/>
                            <Button x:Name="btnClear" Content="🔄" 
                                    Width="40" Height="35"
                                    Background="#607D8B" Foreground="White"
                                    FontWeight="SemiBold" ToolTip="Làm mới form"
                                    Click="btnClear_Click"/>
                        </StackPanel>

                        <!-- STATUS -->
                        <Border Background="#E3F2FD" CornerRadius="5" Padding="10" Margin="0,15,0,0">
                            <TextBlock x:Name="txtStatus" 
                                       Foreground="#1565C0" 
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Text="Sẵn sàng..."/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- RIGHT: DATAGRID + TÌM KIẾM -->
            <Grid Grid.Column="1" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Thanh tìm kiếm -->
                <Border Grid.Row="0" Background="#FAFAFA" CornerRadius="8" Padding="10" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="txtSearch" Width="200" Height="30" 
                                 VerticalContentAlignment="Center" Padding="5"
                                 TextChanged="txtSearch_TextChanged"/>
                        <TextBlock Text="Tìm kiếm theo tên đội..." Foreground="#9E9E9E" 
                                   VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- DataGrid -->
                <Border Grid.Row="1" Background="#FAFAFA" CornerRadius="8" Padding="5">
                    <DataGrid x:Name="dgMatches" 
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionChanged="dgMatches_SelectionChanged"
                              RowHeight="35"
                              AlternatingRowBackground="#E3F2FD"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Đội Nhà" Binding="{Binding HomeTeam}" Width="110"/>
                            <DataGridTextColumn Header="Đội Khách" Binding="{Binding AwayTeam}" Width="110"/>
                            <DataGridTextColumn Header="Ngày" Binding="{Binding MatchDate, StringFormat=dd/MM/yyyy}" Width="90"/>
                            <DataGridTextColumn Header="Kết Quả" Binding="{Binding Result}" Width="70"/>
                            <DataGridTextColumn Header="🟨" Binding="{Binding TotalYellow}" Width="40"/>
                            <DataGridTextColumn Header="🟥" Binding="{Binding TotalRed}" Width="40"/>
                            <DataGridTextColumn Header="Trạng Thái" Binding="{Binding Status}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>

        <!-- FOOTER -->
        <Border Grid.Row="2" Background="#37474F">
            <TextBlock Text="© 2026 - Nguyễn Tấn Đức - Quản lý kết quả trận đấu" 
                       Foreground="#B0BEC5" 
                       FontSize="11"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
